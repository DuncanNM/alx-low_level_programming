#include <stdio.h>
#include "main.h"
/**
 * main - prints absolute value of an integer
 *
 * Return the absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n)
}
